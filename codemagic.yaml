workflows:
  ios-release:
    name: iOS Release
    max_build_duration: 60
    environment:
      groups:
        - ios_signing_credentials

    scripts:
      - name: Initialize secure keychain
        script: keychain initialize

      - name: Clean Pods and reinstall
        script: |
          cd ios
          rm -rf Pods
          rm -rf Podfile.lock
          rm -rf Target\ Support\ Files
          pod deintegrate
          cd ..

      - name: Reinstall CocoaPods and generate workspace
        script: |
          cd ios
          pod repo update
          pod install
          cd ..

      # Fetch signing files ensures your uploaded cert/profile are attached
      - name: Fetch signing files
        script: |
          app-store-connect fetch-signing-files "com.admin.churppyAdmin" \
          --platform IOS \
          --type IOS_APP_STORE \
          --create

      - name: Add signing certificate to keychain
        script: keychain add-certificates

      - name: Apply provisioning profiles automatically
        script: xcode-project use-profiles

      # ✅ Build IPA (auto uses Codemagic’s global cert/profile)
      - name: Build signed IPA
        script: |
          xcode-project build-ipa \
            --workspace "ios/Runner.xcworkspace" \
            --scheme "Runner" \
            --automatic-signing \
            --team-id 7H4229UD7R

    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - flutter_drive.log

    publishing:
      app_store_connect:
        api_key: $APP_STORE_CONNECT_PRIVATE_KEY
        key_id: $APP_STORE_CONNECT_KEY_IDENTIFIER
        issuer_id: $APP_STORE_CONNECT_ISSUER_ID
        submit_to_testflight: true
        submit_to_app_store: false
