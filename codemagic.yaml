workflows:
  ios-release:
    name: iOS Release
    max_build_duration: 60
    environment:
      groups:
        - ios_signing_credentials

    scripts:
      - name: Initialize keychain
        script: keychain initialize

      - name: Clean Pods and reinstall
        script: |
          flutter pub get
          
          # Fix Podfile typo
          sed -i '' 's/generated_xcode_build2_settings_path/generated_xcode_build_settings_path/g' ios/Podfile

          cd ios
          rm -rf Pods Podfile.lock
          pod deintegrate
          pod repo update
          pod install
          cd ..


      - name: Fetch signing files
        script: |
          app-store-connect fetch-signing-files "com.admin.churppyAdmin" \
          --platform IOS \
          --type IOS_APP_STORE \
          --create

      - name: Add signing certificate to keychain
        script: keychain add-certificates

      - name: Apply provisioning profiles to project
        script: xcode-project use-profiles

      - name: Build signed IPA (auto via fetched certs)
        script: |
          echo "🔍 Listing available certs & profiles..."
          ls -la /Users/builder/Library/MobileDevice/Certificates/
          ls -la /Users/builder/Library/MobileDevice/Provisioning\ Profiles/

          echo "🚀 Building and signing IPA using fetched Apple cert/profile..."
          xcodebuild -workspace ios/Runner.xcworkspace \
            -scheme Runner \
            -archivePath build/Runner.xcarchive archive \
            DEVELOPMENT_TEAM=7H4229UD7R \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            PROVISIONING_PROFILE_SPECIFIER="Churppy Admin ios_app_store 1760917692" \
            COMPILER_INDEX_STORE_ENABLE=NO

          echo "📦 Exporting signed IPA..."
          xcodebuild -exportArchive \
            -archivePath build/Runner.xcarchive \
            -exportOptionsPlist /Users/builder/export_options.plist \
            -exportPath build/ios/ipa

          echo "✅ Signed IPA ready at build/ios/ipa/"

    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log

    publishing:
      app_store_connect:
        api_key: $APP_STORE_CONNECT_PRIVATE_KEY
        key_id: $APP_STORE_CONNECT_KEY_IDENTIFIER
        issuer_id: $APP_STORE_CONNECT_ISSUER_ID
        submit_to_testflight: true
        submit_to_app_store: false
